# NRR-2026-023: Four negative findings from a C++–Rust benchmark

## Basic Information

| Field | Content |
|------|------|
| **Entry ID** | NRR-2026-023 |
| **Title** | Four negative findings from a C++–Rust benchmark: the binary-size target was missed, preregistered performance-direction expectations were overturned, sub-microsecond measurements were inconclusive, and the GPU was slower at small batch sizes |
| **Domain** | benchmarking |
| **Category** | hypothesis-falsified |
| **Submitter** | Acerolaorion |
| **Source Project** | etf-pattern-match-pyo3 |
| **Source Authors** | redamancy231-create |
| **Analyst** | Acerolaorion |
| **Date** | 2026-07-29 |

---

## Experiment Overview

### Original Hypothesis

> This entry focuses on three preregistered assumptions: (1) the Rust binary would be less than 1.5× the size of the C++ binary; (2) the single-threaded performance of DTW, `standardize`, and `pattern_match_single` would remain within the preregistered equivalence range; and (3) 100 timed samples would be sufficient to resolve a 10% performance difference. The GPU experiment was added later and had no preregistered threshold.

### Method

Following the NRR Gate 2 preregistration (hash `454873aa`), the C++–Rust benchmark used 20 synthetic-corpus groups and five edge-case corpus groups, five warm-up iterations, 100 timed iterations per case, wrapper-internal timing, and a ±10% equivalence threshold. A separate exploratory GPU test was run on an RTX 4060 Laptop GPU with CuPy 14.1.1 at N = 100, 500, 1,000, and 5,000.

### Expected Result

> The relevant preregistered targets were a binary-size ratio below 1.5×, single-threaded results within the defined equivalence range, and sufficient timing resolution to distinguish a 10% difference. No GPU speedup threshold was preregistered.

### Actual Result

(1) The binary-size ratio was 1.94×, above the preregistered limit of 1.5×. (2) DTW ratios were 0.29–0.39 (Rust was 2.6–3.5× faster); `standardize` was 0.67 and `pattern_match_single` was 0.53, all well outside the expected range. (3) Both cosine medians were 0 ns because of 100 ns timer quantization, and CoV exceeded 5% for the core metrics, so the conclusions were downgraded to indicative. (4) In the exploratory GPU test, end-to-end speedup was 0.2× at N = 100 and 0.7× at N = 500, with break-even near N = 1,000.

### Why Did It Fail?

Pre-registration direction predictions were overly confident in this cross-language benchmark—same algorithm does not guarantee the same performance. Fixed launch and transfer overhead commonly makes GPUs slower for small workloads (0.2× at N = 100 is a textbook case, but seeing it firsthand remains educational). Sub-microsecond measurements require loop amplification rather than independent sampling; at the 100 ns quantization level, 100 samples were insufficient to distinguish a 10% difference.

### What Did We Learn?

1. Pre-registration is not prediction—it exposes blind spots: expectations were overturned for the binary-size ratio, DTW, `standardize`, and `pattern_match_single`
2. In cross-language benchmarks, same algorithm ≠ same performance: differences in allocation behavior, exception-handling semantics, and NumPy interoperability may accumulate as possible contributors
3. Benchmark methodology needs tiering: millisecond tasks (independent sampling works), microsecond tasks (loop amplification needed), sub-microsecond tasks (Criterion-style statistical analysis needed)
4. GPU acceleration has a clear minimum data threshold: at N < 1,000, kernel-launch and PCIe transfer overhead dominate and speedup can fall below 1.0
5. Binary-size expectations must be set with tuning parameters in mind—should not assume Rust ≈ C++ in size without LTO and symbol stripping
